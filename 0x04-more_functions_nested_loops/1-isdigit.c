#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: number to be checked
 * 
 * Return: 1 if c is a digit, otherwise 0
*/
int _isdigit(int c)
{
    return (c >= 48 && c <= 57);
}
