#include <stdio.h>
#include "main.h"

/**
 * main - prints its name
 * @argc: counts the argument
 * @argv: arguments
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[]);
	return (0);
}
