#include "main.h"

/**
 * _islower - check if c is lower case letter or not
 * @c: character to be checked
 *
 * Return: 1 if c is lower case letter, otherwise 0
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
